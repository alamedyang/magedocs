import{_ as a,c as t,o,ah as r}from"./chunks/framework.l_r_SdPt.js";const w=JSON.parse('{"title":"Doors","description":"","frontmatter":{},"headers":[],"relativePath":"doors.md","filePath":"doors.md"}'),i={name:"doors.md"};function n(l,e,s,h,d,c){return o(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="doors" tabindex="-1">Doors <a class="header-anchor" href="#doors" aria-label="Permalink to “Doors”">​</a></h1><h2 id="doors-npcs" tabindex="-1">Doors (NPCs) <a class="header-anchor" href="#doors-npcs" aria-label="Permalink to “Doors (NPCs)”">​</a></h2><p>To make an entity walk through a door, move them to the doorway and then <a href="./hiding_an_entity.html">hide</a> them.</p><p>Entities cannot be unloaded once loaded on a map, so they will always exist no matter what . You might make a different version of the map without such hidden entities, but this would require a fresh <a href="./maps.html#map-loads">map load</a> — which is likely to be inconvenient, especially if multiple entities have to leave at multiple times.</p><h2 id="doors-player" tabindex="-1">Doors (Player) <a class="header-anchor" href="#doors-player" aria-label="Permalink to “Doors (Player)”">​</a></h2><p>For doors to work for the player, you&#39;ll need the following:</p><h3 id="doorway-trigger" tabindex="-1">Doorway Trigger <a class="header-anchor" href="#doorway-trigger" aria-label="Permalink to “Doorway Trigger”">​</a></h3><p>A doorway trigger is a <a href="./vector_objects.html">vector shape</a> placed on the <a href="./maps.html">map</a> wherever you want to trigger warp or doorway behavior, e.g. a literal doorframe, a few tiles spanning the path out of town, etc.</p><p>The vector shape should be large enough that the player can&#39;t skip over it when running past it on low frame rate devices.</p><p>To allow the player access to the doorway, the tile(s) the door is on should not have any <a href="./tilesets.html#tile-collisions">collision data</a>. (If you use a door with alpha on top of a wall tile, for instance, the wall collision might keep the player from physically entering the doorway.)</p><p>Use <a href="./debug_tools.html#vector-view">vector view</a> to debug misbehaving door triggers.</p><h3 id="doorway-watcher" tabindex="-1">Doorway Watcher <a class="header-anchor" href="#doorway-watcher" aria-label="Permalink to “Doorway Watcher”">​</a></h3><p>You can check whether an entity is inside a geometry object with a <a href="./expressions_and_operators.html#bool-operands">bool expression</a> (look for <code>intersects</code>). Daisy chain several <a href="./script_control_flow.html#if-else-chain">ifs</a> to check each vector doorway trigger inside a doorway watcher&#39;s <code>on_tick</code> script. This is best done in the map&#39;s <code>on_tick</code> script, as it cannot be changed by the player using the <a href="./hex_editor.html">hex editor</a>, so is well protected.</p><p>TIP: If there are certain doorways that unlock or lock at specific times in the story, you might want your map&#39;s <code>on_tick</code> script to run logic that determines which watch script to run, and/or include a <a href="./state.html#save-flags">save flag</a> check to give the script an opportunity to jump to the appropriate one automatically if lock/unlock conditions change after the map is loaded.</p><p>When the &quot;entity intersects geometry&quot; condition is met, the doorway watcher script should branch to an appropriate &quot;entering a doorway&quot; script.</p><h3 id="entering-a-doorway-script" tabindex="-1">&quot;Entering a Doorway&quot; Script <a class="header-anchor" href="#entering-a-doorway-script" aria-label="Permalink to “&quot;Entering a Doorway&quot; Script”">​</a></h3><ol><li>If you need to set the <a href="./state.html#warp-state-string">warp_state</a> string for this doorway, set it before anything else.</li><li>The next action depends on the doorway destination: <ol><li><strong>Another spot on the same map</strong>: Teleport the player to the new spot. <ul><li>You will likely still need to have &quot;leaving a doorway&quot; behavior of some kind, though it need not be a separate script.</li></ul></li><li><strong>A different map (e.g. a house, a dungeon, another zone on the overworld)</strong>: The very last action should be <a href="./actions.html#load-map">Load Map</a>. <ul><li>This means all &quot;leaving a doorway&quot; behavior must be handled (or at least triggered) by the target map&#39;s <code>on_load</code> script. See below.</li></ul></li></ol></li></ol><p>Generally, entering a doorway requires no other padding or special handling unless you want to use fades.</p><h3 id="leaving-a-doorway-script" tabindex="-1">&quot;Leaving a Doorway&quot; Script <a class="header-anchor" href="#leaving-a-doorway-script" aria-label="Permalink to “&quot;Leaving a Doorway&quot; Script”">​</a></h3><p>If a new map is loaded:</p><ol><li>Do all other logic checks first, such as for <a href="./commands.html#command-actions">re-registering commands</a> or <a href="./actions.html#position-assignment">teleporting entities</a>. This is vital if the &quot;leaving a doorway&quot; behavior takes any amount of time, i.e. when the entity must walk along a geometry to enter the room.</li><li>Use the <a href="./state.html#warp-state-string">Warp State String</a> to determine which &quot;leaving a doorway&quot; script to run.</li></ol><h4 id="leaving-a-doorway-behavior" tabindex="-1">&quot;Leaving a Doorway&quot; Behavior <a class="header-anchor" href="#leaving-a-doorway-behavior" aria-label="Permalink to “&quot;Leaving a Doorway&quot; Behavior”">​</a></h4><ol><li>Move the player entity to the correct <a href="#spawn-points">spawn point</a>.</li><li>If the default direction of the player entity is different from what you would like, you should either: <ol><li>explicitly turn them the correct direction, or</li><li>have them walk a brief distance in the correct direction out of the doorway.</li></ol></li><li>Turn player control back on.</li></ol><h3 id="doorway-fades" tabindex="-1">Doorway Fades <a class="header-anchor" href="#doorway-fades" aria-label="Permalink to “Doorway Fades”">​</a></h3><p>Fades are a little clunky on the hardware, so if using them for doorways, it might be better to limit them to the most important doorways alone, such as the edges of the map.</p><p>If a fade requires two simultaneous behaviors (e.g. camera fade and entity walk), you will need two script slots. Using the player&#39;s <code>on_tick</code> for this is logical, but not required; feel free to manage the player&#39;s behavior with another entity&#39;s <code>on_tick</code> script, instead.</p><h4 id="entering-a-doorway-script-with-fades" tabindex="-1">Entering a Doorway Script (with Fades) <a class="header-anchor" href="#entering-a-doorway-script-with-fades" aria-label="Permalink to “Entering a Doorway Script (with Fades)”">​</a></h4><ol><li>Disable player control.</li><li>Set the player&#39;s <code>on_tick</code> to a script that walks them toward a vector point several tiles away. <ul><li>To make the player&#39;s walk path as straight as possible, you can put this point far away and set the duration fairly high.</li></ul></li><li>Begin the fade. <ul><li>Set the duration lower than that of the player entity&#39;s walk.</li></ul></li><li>Load the new map.</li></ol><h4 id="leaving-a-doorway-script-with-fades" tabindex="-1">Leaving a Doorway Script (with Fades) <a class="header-anchor" href="#leaving-a-doorway-script-with-fades" aria-label="Permalink to “Leaving a Doorway Script (with Fades)”">​</a></h4><p>Be sure to verify that the new map&#39;s <code>on_load</code> will check the <a href="./state.html#warp-state-string">Warp State String</a> and branch to the the correct &quot;leaving a doorway&quot; script.</p><ol><li>Set the player entity&#39;s <code>on_tick</code> to a script that walks them briefly in a line away from the doorway. <ul><li>For the last action in this <code>on_tick</code> script, goto <a href="./scripts.html#null-script"><code>null_script</code></a> (or set the <code>on_tick</code> to something else) to prevent it from looping.</li></ul></li><li>Fade the screen back in. <ul><li>Duration should be approximately that of the player entity&#39;s walk.</li></ul></li><li>Turn player control back on.</li></ol><h2 id="spawn-points" tabindex="-1">Spawn Points <a class="header-anchor" href="#spawn-points" aria-label="Permalink to “Spawn Points”">​</a></h2><p>For doorway/warp behavior, there are two ways to handle player spawn points:</p><ol><li><strong>Default spawn points</strong>: Player entities will spawn by default where an <code>is_player</code> entity was placed on a <a href="./maps.html">map</a> in Tiled. For maps with a single door, such as an NPC&#39;s house, this will be sufficient to control a player&#39;s spawning behavior — simply place a player entity at the entrance, facing the appropriate direction.</li><li><strong>Custom spawn points</strong>: To control additional spawn points, you will need to create <a href="./vector_objects.html">vector objects</a> to use as teleport destinations.</li></ol><p>To prevent the player from instantly returning to where they came from, the player spawn point should not overlap with the doorway trigger in the new destination.</p>',35)])])}const y=a(i,[["render",n]]);export{w as __pageData,y as default};

import{_ as t,c as i,o as a,ah as n}from"./chunks/framework.l_r_SdPt.js";const m=JSON.parse('{"title":"Hiding an Entity","description":"","frontmatter":{},"headers":[],"relativePath":"hiding_an_entity.md","filePath":"hiding_an_entity.md"}'),o={name:"hiding_an_entity.md"};function r(l,e,h,s,d,p){return a(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="hiding-an-entity" tabindex="-1">Hiding an Entity <a class="header-anchor" href="#hiding-an-entity" aria-label="Permalink to “Hiding an Entity”">​</a></h1><p>There are several ways to hide an <a href="./entities.html">entity</a>:</p><ol><li>Teleport it off the visible <a href="./maps.html">map</a>. <ul><li>In Tiled, place a <a href="./vector_objects.html">vector object</a> off the top-left corner of the map, in the void beyond the map tiles. Teleport entities here to hide them.</li><li>The vector object could be anywhere, really, but due to <a href="./vector_objects.html#coordinate-overflow">coordinate wrapping</a>, the coordinates beyond the top-left corner are actually very, very large numbers, which means those entity tiles won&#39;t draw when viewed from positions with very, very low numbers.</li></ul></li><li>Teleport them behind something opaque.</li><li>Change them into an <a href="./entity_types.html#null-entity">invisible tile</a>.</li></ol><p>After hiding them, you might optionally change their name to something else and change all their <a href="./scripts.html#script-slots">script slots</a> scripts to <a href="./scripts.html#null-script"><code>null_script</code></a>, particularly if there&#39;s a danger for the player to interact with them on accident.</p><p>No matter what, the player will be able to manipulate hidden entities, as they are never properly &quot;unloaded.&quot; This is somewhat authentic in terms of how a real game might handle a similar problem, but if you really, truly want to deny the player access to them, you only have two choices:</p><ol><li>Make a parallel map without them.</li><li><a href="./actors.html">Repurpose</a></li></ol><h2 id="permanently-hiding-entities" tabindex="-1">Permanently Hiding Entities <a class="header-anchor" href="#permanently-hiding-entities" aria-label="Permalink to “Permanently Hiding Entities”">​</a></h2><p>When a map is loaded, entities will spawn in their default state. This includes their map coordinates. In other words, entities you have previously hidden will not remain hidden after the map is reloaded.</p><p>If you want an entity to be hidden permanently, you must manage this with the map&#39;s <code>on_load</code> script and use a <a href="./state.html#save-flags">save flag</a> to determine whether they are hiding.</p><h2 id="un-hiding-an-entity" tabindex="-1">Un-Hiding an Entity <a class="header-anchor" href="#un-hiding-an-entity" aria-label="Permalink to “Un-Hiding an Entity”">​</a></h2><p>If an entity only shows up part way through the game, you must include it in the map from the start and un-hide it at the correct time.</p><p>Depending on your needs, you might either:</p><ol><li>Have the entity offscreen (in the void) by default, and have the map&#39;s <code>on_load</code> teleport it to the correct place if <a href="./state.html#save-flags">conditions</a> have been met.</li><li>Have the entity in the correct place by default, but have the map&#39;s <code>on_load</code> teleport it offscreen if conditions have <em>not</em> been met.</li></ol>',13)])])}const y=t(o,[["render",r]]);export{m as __pageData,y as default};

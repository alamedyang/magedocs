import{_ as e,c as o,o as a,ah as i}from"./chunks/framework.l_r_SdPt.js";const u=JSON.parse('{"title":"Scripts","description":"","frontmatter":{},"headers":[],"relativePath":"scripts.md","filePath":"scripts.md"}'),r={name:"scripts.md"};function s(n,t,c,l,h,p){return a(),o("div",null,[...t[0]||(t[0]=[i('<h1 id="scripts" tabindex="-1">Scripts <a class="header-anchor" href="#scripts" aria-label="Permalink to “Scripts”">​</a></h1><p><a href="./syntax_scopes.html#project-scope">Project scope</a>.</p><p>A script is an array of <a href="./actions.html">actions</a> which will execute one after the other, top to bottom, when the script is run.</p><p>Scripts execute actions until they run out of actions. Importantly, if a script jumps to another script (either with <a href="./actions.html#run-script">Run Script</a> or by having another script set the <a href="#script-slots">script slot</a> to a different script) the current script is <em>completely abandoned</em> and the new script is run instead. It is therefore important to check the order in which actions are given, as any action listed after a script jump will be ignored.</p><p>There is no means for a script to jump to another script and then pick back up where it left off. <a href="./fns.html#fn-call">Fn calls</a> and <a href="./actions.html#copy-script">Copy Script</a> look like function calls, but they actually copy and paste their actions into place; the executing script never leaves its own context in those cases.</p><h2 id="script-definitions" tabindex="-1">Script Definitions <a class="header-anchor" href="#script-definitions" aria-label="Permalink to “Script Definitions”">​</a></h2><p>Defined at <a href="./syntax_scopes.html#syntax-contexts">root level of the file</a>.</p><p><strong>Syntax</strong>: <code>&lt;&quot;script&quot;?&gt; &lt;name: string&gt; { &lt;script body items&gt; }</code></p><h2 id="script-literal" tabindex="-1">Script Literal <a class="header-anchor" href="#script-literal" aria-label="Permalink to “Script Literal”">​</a></h2><p>Scripts can be defined-in-place with script literals in most (though not all) places a script reference is needed.</p><p><strong>Syntax</strong>: <code>&lt;name: string?&gt; { &lt;script body item*&gt; }</code></p><h2 id="script-body-items" tabindex="-1">Script Body Items <a class="header-anchor" href="#script-body-items" aria-label="Permalink to “Script Body Items”">​</a></h2><p>Any of the following:</p><ul><li><a href="./fns.html#fn-call">Fn Call</a></li><li><a href="./macros.html#rand">Rand macro</a></li><li><a href="./macros.html#debug">Debug macro</a></li><li><a href="./actions.html#copy-script">Copy script</a></li><li><a href="./script_control_flow.html#label-definitions">Label definition</a></li><li><a href="./json_literals.html">Json literal</a></li><li><a href="./script_control_flow.html#if-single">If single</a></li><li><a href="./script_control_flow.html#if-else-chain">If/else chain</a></li><li><a href="./script_control_flow.html#while-block">While block</a></li><li><a href="./script_control_flow.html#do-while-block">Do while block</a></li><li><a href="./script_control_flow.html#for-block">For block</a></li><li><a href="./actions.html">Action phrase</a></li></ul><h2 id="null-script" tabindex="-1">Null Script <a class="header-anchor" href="#null-script" aria-label="Permalink to “Null Script”">​</a></h2><p>The only built-in script in the Mage Game Engine is <code>null_script</code>. This script contains no actions. Switching to <code>null_script</code> is a simple way to terminate <code>on_tick</code> scripts, which would otherwise loop forever.</p><p>An <a href="./actions.html">action</a> can set any normal script slot to <code>null_script</code> with any of the normal <a href="./actions.html#script-control">script-controlling actions</a>, and can change itself to <code>null_script</code> by using <code>goto null_script;</code>.</p><p>For patterns that allow script literals, you may instead put an empty script block <code>{}</code> instead.</p><h2 id="script-slots" tabindex="-1">Script Slots <a class="header-anchor" href="#script-slots" aria-label="Permalink to “Script Slots”">​</a></h2><p>Multiple scripts can run at the same time (strictly speaking, they all take turns one after the other during any single game tick), but you cannot arbitrarily spin off threads to run scripts. Instead, entities and maps have &quot;script slots&quot; that are each able to run one script.</p><ul><li>Each <a href="./entities.html">entity</a> has: <ul><li>one <code>on_interact</code> slot</li><li>one <code>on_tick</code> slot</li><li>one <code>on_look</code> slot</li></ul></li><li>The currently-loaded <a href="./maps.html">map</a> has: <ul><li>one <code>on_load</code> slot</li><li>one <code>on_tick</code> slot</li><li>one <code>on_look</code> slot</li></ul></li><li>There is also <a href="./commands.html">command</a> slot for the serial <a href="./terminal.html">terminal</a>.</li></ul><h3 id="on-interact" tabindex="-1"><code>on_interact</code> <a class="header-anchor" href="#on-interact" aria-label="Permalink to “on_interact”">​</a></h3><p>If the player presses the interact button and the interact hitbox hits another entity, that entity&#39;s <code>on_interact</code> script will run. (You can watch this happen with <a href="./debug_tools.html#vector-view">vector view</a>.)</p><p>Scripts run in the <code>on_interact</code> slot will stop once they reach the end of their list of actions. Very commonly, a <a href="./entity_types.html#character-entity">character entity</a>&#39;s <code>on_interact</code> script will be the start script of their <a href="./dialogs.html">dialog</a> tree.</p><p>If the script in this slot jumps to another script at some point, interacting with that entity again will result in the last-used script being run again, not whatever the original <code>on_interact</code> script was. Therefore, if you wish an entity to begin all interactions with the first script in its interact tree, you must explicitly rerset its <code>on_interact</code> script at the end of each of its script branches.</p><h3 id="on-tick" tabindex="-1"><code>on_tick</code> <a class="header-anchor" href="#on-tick" aria-label="Permalink to “on_tick”">​</a></h3><p><code>on_tick</code> scripts continuously evaluate every game tick. Once an <code>on_tick</code> script reaches the end of its list of <a href="./actions.html">actions</a>, the script will return to the beginning of the currently set script and run again on the next game tick.</p><div class="danger custom-block"><p class="custom-block-title custom-block-title-default">DANGER</p><p>This means that if you <code>goto</code> the same script you started from as the <code>on_tick</code> script&#39;s last action, the script slot will NEVER give up its turn! You probably need to use <a href="./actions.html#assign-script-value">Assign Script Value</a> instead, which will set the target script for that slot but NOT immediately execute it like <a href="./actions.html#run-script">Run Script</a> would.</p></div><p>A map&#39;s <code>on_tick</code> script slot is a logical place for a script that watches for whether the player enters a doorway, but <code>on_tick</code> scripts are useful for other kinds of watch scripts, too, such as changing an entity&#39;s idle behavior after a condition has been met.</p><p><code>on_tick</code> slots are what you should use if you want to be able to interrupt or abort a script at an arbitrary place or time e.g. with a button press, or when the player crosses a <a href="./vector_objects.html">collision trigger</a>, etc.</p><p>To terminate an <code>on_tick</code> script, you must use the phrase <code>goto null_script;</code>, or another script must tell the slot to switch to <code>null_script</code>.</p><h3 id="on-load" tabindex="-1"><code>on_load</code> <a class="header-anchor" href="#on-load" aria-label="Permalink to “on_load”">​</a></h3><p>A maps&#39;s <code>on_load</code> script runs when a map is first loaded. Like an <code>on_interact</code> script, once the script reaches the end of its list of actions, the script stops.</p><p>These are useful for identifying and re-implementing the &quot;permanent&quot; changes the player has done on that map, as well as making decisions as to whether a cutscene should be played on that map upon load (e.g. at the beginning of the game).</p><h3 id="on-look" tabindex="-1"><code>on_look</code> <a class="header-anchor" href="#on-look" aria-label="Permalink to “on_look”">​</a></h3><p><code>on_look</code> scripts are run when the player uses the <code>look</code> command in the <a href="./terminal.html">terminal</a>.</p><p><code>look</code> on its own triggers the maps&#39;s <code>on_look</code> script. <code>look</code> + the <a href="./state.html#printing-current-values">current name</a> of an entity will trigger that entity&#39;s <code>on_look</code> script.</p><p><code>on_look</code> scripts can be overridden with a manual <a href="./commands.html">command</a> registration.</p><h3 id="one-slot-one-thread" tabindex="-1">One Slot, One Thread <a class="header-anchor" href="#one-slot-one-thread" aria-label="Permalink to “One Slot, One Thread”">​</a></h3><p>If you try to put all simultaneous behaviors into a single script, the actions will execute one after the other, but only after each action has completely finished. Three entities having a race would instead run the entire course one at a time, each waiting patiently for the previous runner to complete their route. And if one of the simultaneous behaviors has no stopping point, the actions listed afterwards will never execute at all.</p><p>To make a single script perform multiple simultaneous behaviors, it must tell other script slots to start performing those behaviors. The other slots must be <code>on_tick</code> slots because all other types of script slots require player input to start execution, and therefore cannot begin at arbitrary times.</p><p>When the simultaneous behaviors are finished, the <code>on_tick</code> slots must be then set to something else or the new behavior will loop indefinitely (unless that&#39;s what you want). Halt an <code>on_tick</code> script by having that slot <a href="./actions.html#run-script">goto</a> <a href="#null-script"><code>null_script</code></a> as its final action, or by <a href="./actions.html#assign-script-value">setting the target slot</a> goto <code>null_script</code>.</p><p>See <a href="./handlers.html">Handlers</a>.</p>',43)])])}const m=e(r,[["render",s]]);export{u as __pageData,m as default};

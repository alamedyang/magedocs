import{_ as a,c as t,o,ah as i}from"./chunks/framework.l_r_SdPt.js";const p=JSON.parse('{"title":"Encoder","description":"","frontmatter":{},"headers":[],"relativePath":"encoder.md","filePath":"encoder.md"}'),n={name:"encoder.md"};function r(d,e,l,s,c,h){return o(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="encoder" tabindex="-1">Encoder <a class="header-anchor" href="#encoder" aria-label="Permalink to “Encoder”">​</a></h1><p>There are two versions of the encoder, but both produce exactly the same <code>game.dat</code> file.</p><p>What gets encoded:</p><ol><li>Everything listed in <code>scenario.json</code>: <ul><li>dialogSkins</li></ul></li><li>Portraits found in <code>portraits.json</code></li><li>Entities found in <code>entity_types.json</code></li><li>Maps found in <code>maps.json</code></li><li>All MGS files within <code>scenario_source_files/</code></li></ol><p>These items are encoded if they&#39;re being used by the game in some way; a script that isn&#39;t used by any maps, entities, or referenced any included script will be ignored. In other words, entities must be placed on at least one map to be encoded.</p><h2 id="game-dat" tabindex="-1"><code>game.dat</code> <a class="header-anchor" href="#game-dat" aria-label="Permalink to “game.dat”">​</a></h2><p>The <code>game.dat</code> file is the binary file containing all your game scenario content, including images.</p><p>Note that <a href="./state.html#save-data">save data</a> will persist when using the same <code>game.dat</code> file. (Will reflashing the <code>game.dat</code> on the hardware wipe the save data explicitly? Probably. #verifyme — this is correct on the web at least.)</p><p>There is currently no way to bring save data from an old version of the <code>game.dat</code> to a new one, as the data structure of the save data is unlikely to be consistent between versions.</p><h3 id="run-your-game-dat" tabindex="-1">Run Your <code>game.dat</code> <a class="header-anchor" href="#run-your-game-dat" aria-label="Permalink to “Run Your game.dat”">​</a></h3><ul><li><a href="./what_youll_need.html#web-build">Web build</a>: Open the web build in your browser, then drag the <code>game.dat</code> into the browser window. <ul><li>NOTE: If you play the game a little then drag the same <code>game.dat</code> in again, the game will appear to restart afresh — but in fact, save flags and the like may not be reset, <strong>even when you didn&#39;t explicitly save inside the game</strong>. Refresh the browser window to wipe existing game state.</li></ul></li><li><a href="./what_youll_need.html#desktop-build">Desktop build</a>: If you use the <a href="#cli-encoder">CLI encoder</a> to build the game, the <code>game.dat</code> will already be in the appropriate place to run the game. Otherwise, you must move your new <code>game.dat</code> to the <code>MAGE</code> folder.</li><li>Badge: see <a href="./updating_the_hardware.html">Updating the Hardware</a></li></ul><p>Also see: <a href="./debug_tools.html">Debug Tools</a></p><h2 id="cli-encoder" tabindex="-1">CLI Encoder <a class="header-anchor" href="#cli-encoder" aria-label="Permalink to “CLI Encoder”">​</a></h2><p>If you have Node.js installed, you can run the shell script <code>regenerate_dat_file.sh</code> to generate a new <code>game.dat</code> file. There are two versions of this file:</p><ol><li>BM-Badge version: <ul><li><code>cd</code> into <code>MAGE/</code>.</li><li>Run the shell script. The <code>game.dat</code> will be made from the <code>scenario_source_files/</code> in <code>MAGE/</code>.</li></ul></li><li><a href="./mge_vm.html">Sample repo</a> version: <ul><li>first argument: the <code>scenario_source_files/</code> folder to read from</li><li>second argument: where to write the <code>game.dat</code></li><li>This version of the shell script also launches the game automatically.</li></ul></li></ol><p>This encoder is more useful than the web version when you want to test rapid iterations of your game data, as it is less involved. It is also much faster due to its ability to cache encoded graphics.</p><h2 id="web-encoder" tabindex="-1">Web Encoder <a class="header-anchor" href="#web-encoder" aria-label="Permalink to “Web Encoder”">​</a></h2><p>Open <code>SD_Card/MAGE/editor/index.html</code> with a web browser.</p><p>Once the page is open, you can either:</p><ol><li>Drag your <code>scenario_source_files/</code> folder into the window.</li><li>Click the &quot;browse&quot; button and choose the <code>scenario_source_files</code> using your operating system&#39;s file browser.</li></ol><p>Confirm that you want to upload the contents of the folder to your browser, and the encoder will do its magic. If successful, click the &quot;Download game.dat&quot; button, and you&#39;re done!</p><p>The <code>game.dat</code> will be sent to your default download location, so to play it on the desktop build, you&#39;ll first have to move it to the correct place by hand or run the shell script <code>replace_dat_file_with_downloaded.sh</code>. (<code>cd</code> into the <code>SD_Card/MAGE/</code> folder before using the shell script!)</p><p>This encoder is more useful for times you need to <a href="./debug_tools.html">debug something</a>, or when you want to manage an entity&#39;s animations (see below).</p><h3 id="entity-manager" tabindex="-1">Entity Manager <a class="header-anchor" href="#entity-manager" aria-label="Permalink to “Entity Manager”">​</a></h3><p>A special feature of the web version of the encoder is the <a href="./entity_management_system.html">entity management system</a>, which you can use to assign animations to <a href="./entity_types.html#character-entities">character entities</a>.</p><h2 id="debugging" tabindex="-1">Debugging <a class="header-anchor" href="#debugging" aria-label="Permalink to “Debugging”">​</a></h2><p>(Don&#39;t forget that the game has a built-in <a href="./hex_editor.html">hex editor</a>, too!)</p><h3 id="encoder-console" tabindex="-1">Encoder Console <a class="header-anchor" href="#encoder-console" aria-label="Permalink to “Encoder Console”">​</a></h3><p>Both versions of the encoder will tell you when something has gone wrong during the encoding process, and many errors should be self explanatory, e.g. <code>&quot;No object named X could be found on map Y!&quot;</code></p><h3 id="inspecting-wip-data" tabindex="-1">Inspecting WIP Data <a class="header-anchor" href="#inspecting-wip-data" aria-label="Permalink to “Inspecting WIP Data”">​</a></h3><p>You can use the web encoder and your browser&#39;s JavaScript console to inspect various aspects about how the encoder interpreted your game files. (You could even interrupt the encoder <em>while</em> it collects and processes the data.)</p><ul><li>List of all <a href="./state.html#save-flags">save flags</a></li><li>Number of colors in each tileset&#39;s pallet. If trying to reduce the file size of your <code>game.dat</code>, it can help to identify which tilesets have more colors than intended.</li><li>…all the rest #expandme</li></ul><p>This is most useful when the encoder throws an exception, because you can examine the state of data involved in triggering the error — the console messages alone might not identify which script or variable was causing the problem.</p><h3 id="kaitai" tabindex="-1">Kaitai <a class="header-anchor" href="#kaitai" aria-label="Permalink to “Kaitai”">​</a></h3><p>Kaitai (<a href="https://ide.kaitai.io" target="_blank" rel="noreferrer">ide.kaitai.io</a>) is a tool that can parse and analyze binary data formats.</p><p>Available inside your <code>MAGE</code> folder is a file called <code>mage_dat.ksy</code>. You can drag this in a Kaitai window, along with your <code>game.dat</code>, to analyze in great detail the encoded structure of your game.</p><p>This tool lets you easily see the relationship between the hex value of a script/entity_type/dialog/etc. and its name. E.g. if an <a href="./entities.html">entity</a>&#39;s <code>on_tick</code> was being changed unexpectedly, but you only knew the hex value that it&#39;s being changed to (perhaps using the <a href="./hex_editor.html">hex editor</a>, using Kaitai to find the name of the script in question can help you track down the problem.</p>',37)])])}const m=a(n,[["render",r]]);export{p as __pageData,m as default};

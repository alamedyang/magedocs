import{_ as t,c as i,o as a,ah as o}from"./chunks/framework.l_r_SdPt.js";const m=JSON.parse('{"title":"Entities","description":"","frontmatter":{},"headers":[],"relativePath":"entities.md","filePath":"entities.md"}'),n={name:"entities.md"};function r(l,e,s,c,d,h){return a(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="entities" tabindex="-1">Entities <a class="header-anchor" href="#entities" aria-label="Permalink to “Entities”">​</a></h1><p>Entities are a basic element of the Mage Game Engine (MGE).</p><ul><li>All animated graphics must be entities, as ordinary map tiles cannot change appearance or move.</li><li>Any single tile object placed in one of a map&#39;s object layers will be one of the three entity primary id types, automatically determined by the nature of tile being placed.</li><li>All entities have all the same entity properties available to them, visible in-game as a pair of rows on the hex editor in the game — 32 bytes total. <ul><li>These properties are defined when placed in a Tiled map, but they can be modified during gameplay with <a href="./scripts.html">script</a> <a href="./actions.html">actions</a> or by the player via the hex editor.</li></ul></li><li>Entities need not have a name, nor a unique name. Scripts targeting an entity by name will simply use the first the encoder found with that name.</li><li>All entity state (apart from the player&#39;s name) is reset when a map load occurs.</li></ul><h2 id="entity-properties" tabindex="-1">Entity Properties <a class="header-anchor" href="#entity-properties" aria-label="Permalink to “Entity Properties”">​</a></h2><p>Some of these properties are available to entities in Tiled by default.</p><p>To add a property to an entity in Tiled, click the plus at the bottom of the Properties view and set the property type (bool, float, object, string, etc.).</p><p>Many of these may be changed with <a href="./actions.html">actions</a> or with the in-game hex editor.</p><p>The Tiled property name is the same as the MGS keyword unless specified</p><h3 id="name" tabindex="-1">Name <a class="header-anchor" href="#name" aria-label="Permalink to “Name”">​</a></h3><p>These are limited to 12 ASCII characters in the Mage Game Engine.</p><p>An entity&#39;s current name may be printed in a <a href="./dialog_and_serial_dialog_strings.html#print-entity-name">dialog or serial dialog string</a>.</p><ul><li><strong>MGS keyword</strong>: <code>name</code> (string)</li><li><strong>Tiled property name</strong>: <code>Name</code> (string)</li></ul><h3 id="type" tabindex="-1">Type <a class="header-anchor" href="#type" aria-label="Permalink to “Type”">​</a></h3><p>For character entities, this is the <code>type</code>, or the name of the character entity as defined in <code>entity_types.json</code>. This should be automatically assigned by Tiled when you place a character entity on the map. (All tiles on the entity tileset must first have their Type property set to the correct <code>entity_type</code> name for this to work.)</p><ul><li><strong>MGS keyword</strong>: <code>type</code> (string)</li><li><strong>Tiled property name</strong>: <code>Class</code>, formerly <code>Type</code> (string)</li></ul><h3 id="tile-type" tabindex="-1">Tile Type <a class="header-anchor" href="#tile-type" aria-label="Permalink to “Tile Type”">​</a></h3><p><strong><code>primary_id_type</code></strong>, <strong><code>primary_id</code></strong>, and <strong><code>secondary_id</code></strong> are also available to entities, but you need not assign these properties explicitly within Tiled, since they are determined by the identity of the tile you are placing on the map. (See <a href="./entity_types.html">entity_types</a>)</p><ul><li><strong>MGS keywords</strong>: <ul><li><code>primary_id_type</code> (int, u8)</li><li><code>primary_id</code> (int)</li><li><code>secondary_id</code> (int)</li></ul></li></ul><h3 id="path" tabindex="-1">Path <a class="header-anchor" href="#path" aria-label="Permalink to “Path”">​</a></h3><p>You can use this property to assign a vector object to an entity. It&#39;s primarily used so you can give multiple entities the same script, one that compels them to walk along their own path. (Set the target <code>geometry</code> to <code>%ENTITY_PATH%</code> in such scripts.)</p><p>The <a href="./identifiers.html#geometry-identifier">geometry identifier</a> keyword <code>entity_path</code> refers to this assignment.</p><ul><li><strong>MGS keyword</strong>: <code>path</code> (<a href="./identifiers.html#geometry-identifier">geometry identifier</a>)</li><li><strong>Tiled property name</strong>: <code>Path</code> (object)</li></ul><h3 id="position" tabindex="-1">Position <a class="header-anchor" href="#position" aria-label="Permalink to “Position”">​</a></h3><ul><li><strong>MGS keyword</strong>: <ul><li><code>position</code> (movable) <ul><li>used in the <a href="./actions.html">action phrases</a> <a href="./actions.html#position-assignment">Position Assignment</a> and <a href="./actions.html#position-over-time">Position Over Time</a></li></ul></li><li><code>x</code> (int)</li><li><code>y</code> (int)</li></ul></li><li><strong>Tiled property name</strong>: just place the entity where you want it to appear on the map</li></ul><h3 id="direction" tabindex="-1">Direction <a class="header-anchor" href="#direction" aria-label="Permalink to “Direction”">​</a></h3><p>This is the cardinal direction the character entity is facing. This is handled by placing the desired animation tile (and therefore the correct direction) of the target character entity on the map. Left and right can be flipped by taking a side-facing animation tile and checking the <strong>Flipping &gt; Horizontal</strong> checkbox in the Properties view.</p><p>NOTE: Horizontally flipping a front or back-facing character entity will make it appear horizontally flipped within Tiled, but in the MGE this will turn the entity around 180º.</p><p>With scripts, you can have the entity point at something specific using the <a href="./actions.html">action phrase</a> <a href="./actions.html#assign-direction-value">Assign Direction Value</a>, or make a relative turn with <a href="./actions.html#change-int-value">Change Int Value</a>.</p><ul><li><strong>MGS keyword</strong>: <code>direction</code></li><li><strong>Tiled property name</strong>: just place the entity the way you want it to appear on the map</li></ul><h3 id="on-tick-script" tabindex="-1"><code>on_tick</code> Script <a class="header-anchor" href="#on-tick-script" aria-label="Permalink to “on_tick Script”">​</a></h3><p>This identifies the script that is run for that entity every game tick. Since <code>on_tick</code> scripts loop when finished, don&#39;t assign a script you wouldn&#39;t want to run forever.</p><div class="tip custom-block"><p class="custom-block-title">Best Practice</p><p>If you don&#39;t want an entity to have an <code>on_tick</code> script, you can leave this property blank, or avoid adding it altogether. However, it&#39;s useful to set an entity&#39;s <code>on_tick</code> script to <code>null_script</code> if the entity will be given a specific <code>on_tick</code> by another script later in the game. This way, it&#39;s clear that the entity&#39;s <code>on_tick</code> slot is &quot;reserved,&quot; and you won&#39;t accidentally give it an <code>on_tick</code> script that will be overwritten later.</p></div><ul><li><strong>MGS keyword</strong>: <code>on_tick</code> (script slot)</li><li><strong>Tiled property name</strong>: <code>on_tick</code> (string)</li></ul><h3 id="on-interact-script" tabindex="-1"><code>on_interact</code> Script <a class="header-anchor" href="#on-interact-script" aria-label="Permalink to “on_interact Script”">​</a></h3><p>This identifies the script that is run when the player interacts with the entity. Overwhelmingly, this will be the start of the entity&#39;s dialog tree.</p><ul><li><strong>MGS keyword</strong>: <code>on_interact</code> (script slot)</li><li><strong>Tiled property name</strong>: <code>on_interact</code> (string)</li></ul><h3 id="on-look-script" tabindex="-1"><code>on_look</code> Script <a class="header-anchor" href="#on-look-script" aria-label="Permalink to “on_look Script”">​</a></h3><p>This identifies the script that is run when the player interacts with the entity using the terminal via <code>look at &lt;entity name&gt;</code>.</p><ul><li><strong>MGS keyword</strong>: <code>on_look</code> (script slot)</li><li><strong>Tiled property name</strong>: <code>on_look</code> (string)</li></ul><h3 id="current-animation" tabindex="-1">Current Animation <a class="header-anchor" href="#current-animation" aria-label="Permalink to “Current Animation”">​</a></h3><p>This lets you set the specific animation for character entities. <code>0</code> is the idle, and will play by default.</p><p>Animations you choose in Tiled via tile selection are ignored.</p><ul><li><strong>MGS keyword</strong>: <code>current_animation</code> (int, u8)</li></ul><h3 id="current-frame" tabindex="-1">Current Frame <a class="header-anchor" href="#current-frame" aria-label="Permalink to “Current Frame”">​</a></h3><p>This lets you start an entity&#39;s animation at an arbitrary frame. This is useful for staggering entities with identical animation timings.</p><ul><li><strong>MGS keyword</strong>: <code>current_frame</code> (int, u8)</li><li><strong>Tiled property name</strong>: <code>current_frame</code> (number)</li></ul><h3 id="glitched" tabindex="-1">Glitched <a class="header-anchor" href="#glitched" aria-label="Permalink to “Glitched”">​</a></h3><p>This lets you start an entity&#39;s animation at an arbitrary frame. This is useful for staggering entities with identical animation timings.</p><ul><li><strong>MGS keyword</strong>: <code>glitched</code> (bool)</li><li><strong>Tiled property name</strong>: <code>is_glitched</code> (bool)</li></ul><h3 id="debug" tabindex="-1">Debug <a class="header-anchor" href="#debug" aria-label="Permalink to “Debug”">​</a></h3><p>This indicates whether the entity is hidden when <a href="./debug_tools.html#debug-mode">Debug Mode</a> is off. (While this can be toggled in the hex editor within the MGE, in practice it will not actually affect anything in-game, as the map is reloaded afresh when debug mode is toggled.)</p><ul><li><strong>MGS keyword</strong>: n/a</li><li><strong>Tiled property name</strong>: <code>is_debug</code> (bool)</li></ul><h3 id="strafe" tabindex="-1">Strafe <a class="header-anchor" href="#strafe" aria-label="Permalink to “Strafe”">​</a></h3><p>This adds a rotation to an entity&#39;s animations. This is different from turning an entity toward something or someone; this action applies a rotation to <em>all</em> an entity&#39;s animations, including while the entity is in motion. In short, use this action to make an entity walk backwards or strafe (walk sideways).</p><p>This number cannot be negative.</p><p>This value cannot be checked (used in an int expression), only set.</p><ul><li><strong>MGS keyword</strong>: <code>strafe</code> (int)</li><li><strong>Tiled property name</strong>: n/a</li></ul><h3 id="player" tabindex="-1">Player <a class="header-anchor" href="#player" aria-label="Permalink to “Player”">​</a></h3><p>This is the entity the player will control within the map. There should be only one such entity per map. (If there is more than one, the encoder will throw an error.)</p><ul><li><strong>MGS keyword</strong>: n/a</li><li><strong>Tiled property name</strong>: <code>is_player</code> (bool)</li></ul><p>Without an <code>is_player</code> entity:</p><ol><li>The camera will be positioned with its top-left corner aligned with the top-left corner of the map&#39;s coordinate space.</li><li>The left joystick will control the camera movement. <ul><li>Holding <code>X</code> (the run button) will make the camera move faster.</li></ul></li><li>The hex editor can still be engaged.</li><li><a href="./debug_tools.html#vector-view">Vector view</a> can still be toggled (<code>XOR</code> + <code>MEM0</code>, or <code>F1</code> + <code>F5</code> on desktop).</li><li><a href="./maps.html#map-loads">Map loads</a> work, but the camera will remain wherever it was last positioned. (It will not be reset to what is described in #1.)</li><li><a href="./dialogs.html">dialogs</a> referencing the player entity will use a random portrait and the name <code>MISSING: 253</code>.</li><li><a href="./actions.html">actions</a> targeting the player entity will generally do nothing. <ul><li>If you want to change the player&#39;s name via an action (e.g. in your game&#39;s main menu), this means you must have an <code>is_player</code> entity somewhere on the map.</li></ul></li></ol>',62)])])}const y=t(n,[["render",r]]);export{m as __pageData,y as default};

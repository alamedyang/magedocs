import{_ as o,c as t,o as i,ah as s}from"./chunks/framework.l_r_SdPt.js";const u=JSON.parse('{"title":"TODO","description":"","frontmatter":{},"headers":[],"relativePath":"todos.md","filePath":"todos.md"}'),a={name:"todos.md"};function n(l,e,r,d,c,h){return i(),t("div",null,[...e[0]||(e[0]=[s('<h1 id="todo" tabindex="-1">TODO <a class="header-anchor" href="#todo" aria-label="Permalink to “TODO”">​</a></h1><ul><li><code>delete command _ fail</code> is missing in the grammar!</li><li>could add sigil to <code>debug!()</code> serial dialog references, so we can use strings instead of being limited to barewords?</li><li>Are recursive fn calls detected?</li><li>Can fn calls use bool expressions as args? Should they?</li><li>If dialogSkins are the only things left in <code>scenario.json</code> should it be moved to its own file, and <code>scenario.json</code> retired?</li><li>Change <code>RNG!()</code> to <code>roll!()</code> (?) (helps keep it straight from <code>rand!()</code></li><li>Change <code>include</code> back to <code>include!()</code> so it matches the other &quot;macros&quot;</li><li>it&#39;s annoying to remember which things don&#39;t take a <code>;</code> (or which bracketed things do, like in show dialog blocks), so we might make them optional in certain common situations so it&#39;s easier to err on the side of always putting them. (Making them optional everywhere breaks too much; I tried.)</li><li>Make it so ints can have optional sigils? (A quick attempt didn&#39;t work)</li><li>Test expansion-ness of push_left and push</li><li>Do custom ANSI sequences come through?</li><li>Currently, fns require at least one argument; the parser distinuishes fn calls and copy script based on whether there&#39;s any args passed. This was done because it was confusing when one resembled a normal function call and the other didn&#39;t when the two were otherwise used the same way. However, if this paradigm too proves frustrating (i.e. if it turns out we frequently want to make fns with no args), we can disambuguate the copy script and fn call syntax again, or make the parser more discerning re: deciding between namespaces.</li><li>What happens in debug mode if a script targets a debug entity?</li><li>Is <code>SET_HEX_EDITOR_STATE</code> working? I think it is?</li><li>Can all three of map&#39;s slots be paused or what?</li><li>Does LOOP_CAMERA_ALONG_GEOMETRY work?</li><li>Can you set serial connect message to a serial dialog literal?</li><li>do the <code>on_look</code> slots use the <code>command</code> slot? They have their own space on the entity struct, so....</li></ul>',2)])])}const g=o(a,[["render",n]]);export{u as __pageData,g as default};

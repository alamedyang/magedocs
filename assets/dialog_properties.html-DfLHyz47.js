import{_ as e,o as t,c as a,a as o}from"./app-oGoBbKgC.js";const i={},n=o('<h1 id="dialog-properties" tabindex="-1"><a class="header-anchor" href="#dialog-properties" aria-hidden="true">#</a> Dialog Properties</h1><p>Also see: <a href="../dialogs_json">Dialogs (JSON)</a>, <a href="../mgs/dialogs_mgs">Dialogs (MGS)</a></p><p><a href="../dialogs">Dialogs</a> must, at bare minimum, have <code>messages</code>. If no <code>name</code> or <code>entity</code> is provided (or is set to an empty string <code>&quot;&quot;</code>), the dialog box label will not appear.</p><h2 id="alignment" tabindex="-1"><a class="header-anchor" href="#alignment" aria-hidden="true">#</a> <code>alignment</code></h2><p><code>alignment</code> controls the position of the dialog box and its portrait/label boxes. Valid choices:</p><ul><li><code>BOTTOM_LEFT</code></li><li><code>BOTTOM_RIGHT</code></li><li><code>TOP_LEFT</code></li><li><code>TOP_RIGHT</code></li></ul><p>In <a href="../mgs/mgs_natlang">MGS Natlang</a>, these can be abbreviated to <code>BL</code>, <code>TR</code>, etc.</p><p>For the DC801 black mage game, dialog alignment should be <code>BOTTOM_RIGHT</code> for the player character, and <code>BOTTOM_LEFT</code> for everything else. <code>TOP_LEFT</code> and <code>TOP_RIGHT</code> are only used when an entity is positioned in such a way that it would most likely be obscured by the dialog box itself.</p><p><a href="../mgs/mgs_natlang">MGS Natlang</a> will use <code>BOTTOM_LEFT</code> if this property is not provided. A default can also be set for each MGS file individually using <code>settings for dialog {}</code>, or in multiple files when combined with <code>include!()</code>.</p><h2 id="entity" tabindex="-1"><a class="header-anchor" href="#entity" aria-hidden="true">#</a> <code>entity</code></h2><p>Optional. <code>entity</code> refers to a specific entity by its <strong>given name</strong> (the name it was given within Tiled). This is technically optional, but you will almost certainly want to include it.</p><p>This property can be any of the following:</p><ol><li><a href="../entities/PLAYER"><code>%PLAYER%</code></a>, which refers to the player entity</li><li><a href="../entities/SELF"><code>%SELF%</code></a>, which refers to the entity running the script</li><li>the <strong>given name</strong> of an entity (no percent signs)</li></ol><p>The MGE will use the referenced entity&#39;s <a href="../scripts/printing_current_values">current name</a> for the dialog label automatically.</p><p>For the <code>entity</code> property, you must not use percent signs to refer to entities, or the <a href="../encoder/mge_encoder">MGE encoder</a> will give you a message in the vein of <code>No entity named %Helga% found on map default!</code> If you don&#39;t want to use an entity&#39;s <a href="../scripts/printing_current_values">current name</a> in the dialog label, you must use the <code>name</code> property, which can print a string literally.</p><p>If the referenced entity is a <a href="../entities/character_entity">character entity</a>, the <a href="../encoder/mge_encoder">MGE Encoder</a> will use <a href="../structure/entity_types.json"><code>entity_types.json</code></a> to automatically determine the portrait image.</p><h2 id="name" tabindex="-1"><a class="header-anchor" href="#name" aria-hidden="true">#</a> <code>name</code></h2><p>Optional. <code>name</code> provides a string for dialog box label, and if included, it will override any names inherited from <code>entity</code> above.</p><p>This property can be any of the following:</p><ol><li><a href="../entities/PLAYER"><code>%PLAYER%</code></a>, which refers to the player entity</li><li><a href="../entities/SELF"><code>%SELF%</code></a>, which refers to the entity running the script</li><li>an arbitrary ASCII string up to 12 characters long</li><li>the <strong>given name</strong> of an entity, but enclosed in percent signs: <code>%Entity Name%</code></li></ol><p>If you use the entity&#39;s given name without percent signs, it will behave as a static string #3. This will appear to behave the same as #4 in most cases, but #4 will allow inherited dialog labels to reflect the <a href="../scripts/printing_current_values">entity&#39;s name as it currently exists in RAM</a> and not the name it was originally assigned.</p><h2 id="portrait" tabindex="-1"><a class="header-anchor" href="#portrait" aria-hidden="true">#</a> <code>portrait</code></h2><p>Optional. This is the name of the portrait defined in <a href="../structure/portraits.json"><code>portraits.json</code></a> you want to use for the dialog. This will override any portraits inherited from <code>entity</code> above.</p><p>If the dialog cannot find a portrait to use (either via <code>portrait</code> or <code>entity</code>), then the dialog will display no portrait box.</p><h2 id="border-tileset" tabindex="-1"><a class="header-anchor" href="#border-tileset" aria-hidden="true">#</a> <code>border_tileset</code></h2><p>Optional. This is name of the dialog box tileset you want the dialog box to use, if not the default. (Default is the first border tileset listed in <a href="../structure/scenario.json"><code>scenario.json</code></a>.)</p><h2 id="emote" tabindex="-1"><a class="header-anchor" href="#emote" aria-hidden="true">#</a> <code>emote</code></h2><p>Optional. Allows you to select a specific emote within the entity&#39;s entry in <a href="../structure/portraits.json"><code>portraits.json</code></a>. The default emote (<code>0</code>) will display if not specified. (While emotes can have assigned names, you must use intigers to control them here.)</p><h2 id="messages" tabindex="-1"><a class="header-anchor" href="#messages" aria-hidden="true">#</a> <code>messages</code></h2><p>Each dialog message is limited to five rows of 42 ASCII characters. <a href="../mgs/mgs_natlang">MGS Natlang</a> will wrap this text automatically, though you can hard wrap it wherever you want with <code>\\n</code>.</p><h3 id="manual-text-wrapping-json" tabindex="-1"><a class="header-anchor" href="#manual-text-wrapping-json" aria-hidden="true">#</a> Manual Text Wrapping (JSON)</h3><p>Excess text will wrap awkwardly off the screen unless wrapped with line breaks (<code>\\n</code>).</p><p>Do not use a space after <code>\\n</code> unless you want the next line to begin with a space. Therefore, when manually wrapping text, you will likely use <code>\\n</code> to replace a space character entirely, which can be difficult to read, e.g.:</p><p><code>&quot;Hey, there! This line is really long and\\nwill need to be hard wrapped.&quot;</code></p><blockquote><p>Hey, there! This line is really long and<br> will need to be hard wrapped.</p></blockquote><p>Smart quotes are not a part of ASCII, nor are ellipses (…), so when copying text written in word processors, please take care to replace any such characters with ASCII equivalents.</p><p>To place a double quote within a message, you must type a backslash first: <code>\\&quot;</code></p><p>Example:</p><p><code>&quot;That&#39;s not what he meant by \\&quot;safe,\\&quot; is it?&quot;</code></p><blockquote><p>That&#39;s not what he meant by &quot;safe,&quot; is it?</p></blockquote><p>When using backslash with a special character (like the double quotes above), consider it to be 1 character and not 2 when calculating text wrapping. E.g., the dialog message above is 44 lines in the dialog JSON file, but only 42 characters when displayed in the dialog box, so the message will not need to be hard wrapped.</p><p>Many text editors (such as Visual Studio Code) can tell you how many characters you currently have selected, which makes it easy to determine where line breaks should be inserted.</p>',42),r=[n];function s(d,l){return t(),a("div",null,r)}const h=e(i,[["render",s],["__file","dialog_properties.html.vue"]]);export{h as default};

import{_ as e,o as t,c as o,a}from"./app-7-tBH-Mq.js";const i={},n=a('<h1 id="script-slots" tabindex="-1"><a class="header-anchor" href="#script-slots" aria-hidden="true">#</a> Script Slots</h1><p>Multiple <a href="../scripts">script</a> can run at the same time (strictly speaking, they all take turns one after the other during any single game tick), but you cannot arbitrarily run an indefinite number of scripts. Instead, entities and maps have &quot;script slots&quot; that are each able to run one script.</p><ul><li>Each <a href="../entities">entity</a> has: <ul><li>one <a href="../scripts/on_interact"><code>on_interact</code></a> slot</li><li>one <a href="../scripts/on_tick"><code>on_tick</code></a> slot</li><li>one <a href="../scripts/on_look"><code>on_look</code></a> slot</li></ul></li><li>The currently-loaded <a href="../maps">map</a> has: <ul><li>one <a href="../scripts/on_load"><code>on_load</code></a> slot</li><li>one <a href="../scripts/on_tick"><code>on_tick</code></a> slot</li><li>one <a href="../scripts/on_look"><code>on_look</code></a> slot</li></ul></li><li>There is also <a href="../hardware/commands">command</a> slot for the serial <a href="../hardware/terminal">terminal</a>.</li></ul><p>#verifyme — do the <code>on_look</code> slots use the <code>command</code> slot? They have their own space on the entity struct, so....</p><h2 id="one-slot-one-script" tabindex="-1"><a class="header-anchor" href="#one-slot-one-script" aria-hidden="true">#</a> One Slot, One Script</h2><p>Importantly, a script slot can only run one script — if a script jumps to another script (either with <a href="../actions/RUN_SCRIPT">RUN_SCRIPT</a> or by <a href="../actions/conditional_gotos">script jumping via some kind of logic check</a>) the current script is <em>completely abandoned</em> and the new script is run instead. It is therefore important to check the order in which actions are given, as any action listed after a script change will be ignored.</p><p>There is no nested callback structure, no child function returning something to its parent function, nor anything like that.</p><p>Nor is it possible to do more than one logic check simultaneously. If you want to check multiple conditions at once, you must branch to a different script for each aspect of the fail condition and let the remainder of the original script contain the actions for the success condition. <a href="../mgs/mgs_natlang">MGS Natlang</a> simplifies this a little bit by allowing multiple conditions to be written per behavior block, but these conditions can only be linked with OR (<code>||</code>) not AND (<code>&amp;&amp;</code>) for this reason; the <a href="../mgs/advanced_syntax/if_and_else">if and else</a> macro expands each OR condition into an individual logic check and jump, jumping to a common <a href="../mgs/advanced_syntax/labels">label</a> index for all shared <code>{}</code> behavior.</p>',8),s=[n];function c(r,l){return t(),o("div",null,s)}const d=e(i,[["render",c],["__file","script_slots.html.vue"]]);export{d as default};

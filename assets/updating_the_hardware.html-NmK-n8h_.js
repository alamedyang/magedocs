import{_ as e,o as a,c as n,a as s}from"./app-60F4FVZl.js";const t={},o=s(`<h1 id="updating-the-hardware" tabindex="-1"><a class="header-anchor" href="#updating-the-hardware" aria-hidden="true">#</a> Updating the Hardware</h1><h2 id="game-dat" tabindex="-1"><a class="header-anchor" href="#game-dat" aria-hidden="true">#</a> <code>game.dat</code></h2><p>To run the game on the hardware, prepare a microSD card (FAT32) with a folder called <code>MAGE</code> in the root directory. Copy the <code>game.dat</code> into <code>MAGE/</code>, then insert the card into the slot on the hardware.</p><p>If the hardware determines its <code>game.dat</code> is different from the one on the microSD card (or if you hold <code>MEM3</code> when turning on the badge), you will see:</p><div class="language-text" data-ext="text"><pre class="shiki dark-plus" style="background-color:#1E1E1E;" tabindex="0"><code><span class="line"><span style="color:#D4D4D4;">The file \`game.dat\` on your SD card does not match</span></span>
<span class="line"><span style="color:#D4D4D4;">what is on your badge ROM chip.</span></span>
<span class="line"><span style="color:#D4D4D4;"></span></span>
<span class="line"><span style="color:#D4D4D4;"> SD hash: [game.dat checksum #1]</span></span>
<span class="line"><span style="color:#D4D4D4;">ROM hash: [game.dat checksum #2]</span></span>
<span class="line"><span style="color:#D4D4D4;"></span></span>
<span class="line"><span style="color:#D4D4D4;">Would you like to update your scenario data?</span></span>
<span class="line"><span style="color:#D4D4D4;">------------------------------------------------</span></span>
<span class="line"><span style="color:#D4D4D4;"></span></span>
<span class="line"><span style="color:#D4D4D4;">&gt; Press MEM0 to cancel</span></span>
<span class="line"><span style="color:#D4D4D4;"></span></span>
<span class="line"><span style="color:#D4D4D4;">&gt; Press MEM3 to update the ROM</span></span>
<span class="line"><span style="color:#D4D4D4;"></span></span></code></pre></div><p>Press <code>MEM3</code> (lowest button on the right side of the screen) to tell the hardware to flash the new <code>game.dat</code>. The time it takes to copy will depend on the size of the <code>game.dat</code>, but it should be somewhere in the ballpark of 20 seconds.</p><h2 id="game-engine" tabindex="-1"><a class="header-anchor" href="#game-engine" aria-hidden="true">#</a> Game Engine</h2><p>To start the game in bootloader mode, hold down the hex digit <code>1</code> button while you power on the hardware. If bootloader mode was successfully triggered, the screen will be white and the LEDs will blink in a circular pattern.</p><p>If the hardware is plugged into a computer while it is in bootloader mode, it will appear as a drive called <code>DC801BOOT</code> (or <code>NRF52BOOT</code>). Copy the new game engine UF2 (the filename doesn&#39;t matter) onto the drive.</p><p>Once the UF2 is copied, the drive will disconnect and the badge will reboot automatically.</p>`,10),d=[o];function l(r,c){return a(),n("div",null,d)}const h=e(t,[["render",l],["__file","updating_the_hardware.html.vue"]]);export{h as default};

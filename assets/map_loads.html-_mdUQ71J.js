import{_ as e,o as a,c as t,a as r}from"./app-7-tBH-Mq.js";const s={},o=r('<h1 id="map-loads" tabindex="-1"><a class="header-anchor" href="#map-loads" aria-hidden="true">#</a> Map Loads</h1><p>The first map listed in <a href="../structure/maps.json"><code>maps.json</code></a> is the map the game will load when first turned on. If you want the game to start on a different map (perhaps skipping an intro <a href="../techniques/cutscenes">cutscene</a> the player has seen before), you should handle this logic checking on the <a href="../maps">maps</a>&#39; <a href="../scripts/on_load"><code>on_load</code></a> <a href="../scripts">script</a>, and <a href="../actions/LOAD_MAP">load a different map</a> as appropriate.</p><p>Maps can be assigned an <code>on_load</code> script, which will run once when a map is loaded.</p><h2 id="to-reload-the-current-map" tabindex="-1"><a class="header-anchor" href="#to-reload-the-current-map" aria-hidden="true">#</a> To Reload the Current Map</h2><ul><li>Toggle <a href="../debug/debug_mode">debug mode</a>: <code>XOR</code> + <code>MEM1</code> (or <code>F1</code> + <code>F6</code> on desktop)</li><li>Soft reset current map: <code>XOR</code> + <code>MEM3</code> (or <code>F1</code> + <code>F8</code> on desktop)</li><li>Target the current map with <a href="../actions/LOAD_MAP">LOAD_MAP</a></li></ul><h2 id="game-state" tabindex="-1"><a class="header-anchor" href="#game-state" aria-hidden="true">#</a> Game State</h2><h3 id="preserved" tabindex="-1"><a class="header-anchor" href="#preserved" aria-hidden="true">#</a> Preserved</h3><p><a href="../scripts/save_data">Save data</a>, including the player name, persists between map loads, but no other entity state is preserved.</p><h3 id="not-preserved" tabindex="-1"><a class="header-anchor" href="#not-preserved" aria-hidden="true">#</a> Not Preserved</h3><p>All <a href="../entities">entity</a> state is wiped apart from the player name. This includes every entity&#39;s appearance, <a href="../scripts/script_slots">script slots</a> (both their script assignments and their progress within their scripts), and location.</p><p>All <a href="../hardware/terminal">terminal</a> <a href="../hardware/commands">commands</a> are reset. For a command that needs to persist throughout the game, you must initialize them afresh at the beginning of the map load. (We recommend using <a href="../actions/COPY_SCRIPT">COPY_SCRIPT</a> on a separate <a href="../techniques/map_initialization_scripts">map initialization script</a> so that such logic checks can be shared between all relevant maps.)</p>',11),i=[o];function n(d,h){return a(),t("div",null,i)}const p=e(s,[["render",n],["__file","map_loads.html.vue"]]);export{p as default};

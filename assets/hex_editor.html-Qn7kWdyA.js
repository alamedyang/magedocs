import{_ as e,o as t,c as o,a}from"./app-GYgAq86M.js";const n={},h=a('<h1 id="hex-editor" tabindex="-1"><a class="header-anchor" href="#hex-editor" aria-hidden="true">#</a> Hex Editor</h1><p>The hex editor is a unique feature of the Mage Game Engine (MGE) that allows you to view and edit <a href="entity_properties">entity properties</a> for <a href="entities">entities</a> in the live game.</p><p>If hex editor control is turned off, the hex editor will not open and values cannot be changed.</p><h2 id="opening-the-hex-editor" tabindex="-1"><a class="header-anchor" href="#opening-the-hex-editor" aria-hidden="true">#</a> Opening the Hex Editor</h2><p>Press the capacitive button (the hat) on the top of the physical badge to open the hex editor. (On the web, press <code>ESC</code> or <code>TAB</code> or click the button in the upper-left corner.) Repeat to close the hex editor again.</p><p>Remember that you can select a byte in the hex editor and watch its value change (or change it yourself) live in the overworld — you need not open the hex editor all the time to monitor a single value while playing the game.</p><h2 id="selecting-a-value" tabindex="-1"><a class="header-anchor" href="#selecting-a-value" aria-hidden="true">#</a> Selecting a Value</h2><p>There are several methods:</p><ol><li>Once inside the hex editor, use the arrow joystick (on the web, use the arrow keys or the WSAD keys) to control which byte is selected.</li><li>In the overworld, you can face an entity and press the triangle button to open the hex editor and jump to the first byte of that entity&#39;s struct.</li><li>Use the <code>MEM0</code> through <code>MEM3</code> buttons to jump to a saved byte offset within the currently-selected entity struct. <ul><li>These saved offsets can be changed by holding <code>PAGE</code> and pressing the target <code>MEM</code> button.</li></ul></li></ol><h2 id="changing-a-value" tabindex="-1"><a class="header-anchor" href="#changing-a-value" aria-hidden="true">#</a> Changing a Value</h2><p>The <code>XOR</code>, <code>ADD</code>, and <code>SUB</code> buttons on the left side of the screen change the operator mode for the bit buttons below the screen. E.g. to add 4 to the value, set the mode to <code>ADD</code> and press the <code>4</code> bit button.</p><p>While inside the hex editor you can also increment and decrement the selected value by 1 using the triangle and X buttons.</p>',12),i=[h];function r(d,c){return t(),o("div",null,i)}const l=e(n,[["render",r],["__file","hex_editor.html.vue"]]);export{l as default};

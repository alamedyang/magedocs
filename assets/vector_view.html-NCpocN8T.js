import{_ as e,o as t,c as i,a}from"./app-oGoBbKgC.js";const o={},r=a('<h1 id="vector-view" tabindex="-1"><a class="header-anchor" href="#vector-view" aria-hidden="true">#</a> Vector View</h1><p>Vector view, one of the <a href="../debug/debug_tools">debug tools</a> in the game, is triggered in-game by pressing <code>XOR</code> and <code>MEM0</code> (the top two buttons on each side of the screen) at the same time. On desktop, press <code>F1</code> and <code>F5</code> instead.</p><p>NOTE: Currently, vector view cannot be toggled when <a href="../hardware/hex_editor">hex editor</a> <a href="../actions/SET_HEX_EDITOR_STATE">control</a> is off.</p><h2 id="visible-in-vector-view" tabindex="-1"><a class="header-anchor" href="#visible-in-vector-view" aria-hidden="true">#</a> Visible in Vector View</h2><h3 id="vector-objects" tabindex="-1"><a class="header-anchor" href="#vector-objects" aria-hidden="true">#</a> <a href="../maps/vector_objects">Vector Objects</a></h3><ul><li>vector paths</li><li>vector shapes</li><li>vector points (displayed as Xs)</li></ul><p>These lines are green ordinarily, but will turn red if the player entity is &quot;inside&quot; them.</p><h3 id="entity-vectors" tabindex="-1"><a class="header-anchor" href="#entity-vectors" aria-hidden="true">#</a> Entity Vectors</h3><p>Entity hit boxes are half the length and half the height of the <a href="../entities">entity</a>&#39;s tile size, and are positioned at the bottom-center of the sprite. This box is green by default, but the entity that was most recently <a href="../scripts/on_interact">interacted</a> with (either with the interact button or hack button) will have a red hitbox instead.</p><p>The entity&#39;s position is considered to be the center of its hitbox as defined above, and is marked by a blue X.</p><h3 id="interaction-hitboxes" tabindex="-1"><a class="header-anchor" href="#interaction-hitboxes" aria-hidden="true">#</a> Interaction Hitboxes</h3><p>When the player presses the hack button or <a href="../scripts/on_interact">interact</a> button, a rectangle is cast in front of the player entity. After a successful interaction, the hitbox will be blue, and will be yellow otherwise.</p><h3 id="collision-vectors" tabindex="-1"><a class="header-anchor" href="#collision-vectors" aria-hidden="true">#</a> Collision Vectors</h3><p>Tiles on the map will have their <a href="../tilesets/tile_collisions">vector collision shapes</a> drawn in green, unless the engine has determined they are close enough to the player that collision for that tile will need to be checked, after which they will be drawn in yellow. Red lines indicate a collision was detected on that tile.</p><p>The player&#39;s collision spokes (drawn in purple) are projected in front of the player entity. When they cross a tile&#39;s collision geometry, a collision is detected, and the corresponding knockback vector is drawn as a red line extending in the opposite direction.</p><h3 id="collision-details" tabindex="-1"><a class="header-anchor" href="#collision-details" aria-hidden="true">#</a> Collision details</h3><p>In the upper-left corner of the screen is a more detailed view of the actual math behind the collision algorithm. Map makers creating original scenarios can safely ignore this part of vector view.</p>',17),n=[r];function s(h,c){return t(),i("div",null,n)}const d=e(o,[["render",s],["__file","vector_view.html.vue"]]);export{d as default};

import{_ as e,o as a,c as t,a as s}from"./app-j-XjLWpf.js";const i={},o=s(`<h1 id="debug-tools" tabindex="-1"><a class="header-anchor" href="#debug-tools" aria-hidden="true">#</a> Debug Tools</h1><h2 id="debug-mode" tabindex="-1"><a class="header-anchor" href="#debug-mode" aria-hidden="true">#</a> Debug Mode</h2><p>Debug mode is triggered in-game by pressing <code>XOR</code> and <code>MEM1</code> (the top button on the left of the screen and the second button on the right) at the same time. On desktop: press <code>F1</code> and <code>F6</code> instead. This counts as a <a href="map_loads">map reload</a>.</p><p>Turn off debug mode in the same way.</p><h3 id="debug-entities" tabindex="-1"><a class="header-anchor" href="#debug-entities" aria-hidden="true">#</a> Debug Entities</h3><p>Normally, the Mage Game Engine (MGE) omits <a href="entities">entities</a> with the <code>is_debug</code> value of <code>true</code> when loading <a href="maps">maps</a>. Such entities effectively do not exist in &quot;production mode,&quot; and therefore will not be included in the list of entities in the <a href="hex_editor">hex editor</a>, will not appear anywhere on the map, cannot be the target of <a href="scripts">script</a>, etc. (#verifyme) When debug mode is activated, however, the <a href="map_loads">current map is reloaded</a> with <code>is_debug</code> entities included.</p><p>The chapter 1 version of the engine <em>must</em> use debug entities to trigger debug scripts, as the serial <a href="terminal">terminal</a> was not implemented yet, and there was not yet an action to check whether debug mode is on.</p><div class="custom-container tip"><p class="custom-container-title">Best Practice</p><p>When making debug entities, it helps a lot to give them dialog describing what they are doing to change the game state. Better still is putting the debug behavior behind a <a href="dialogs#options">multiple choice dialog</a> so that the debug entity can be disengaged without making any changes in case it is ever engaged by accident.</p></div><h3 id="debug-scripting" tabindex="-1"><a class="header-anchor" href="#debug-scripting" aria-hidden="true">#</a> Debug Scripting</h3><p>With <a href="actions/CHECK_DEBUG_MODE">CHECK_DEBUG_MODE</a>, you can add additional behavior to your game that is easy to enable when play testing but hidden from players by default.</p><h4 id="debug-logging" tabindex="-1"><a class="header-anchor" href="#debug-logging" aria-hidden="true">#</a> Debug Logging</h4><p>The <a href="mgs/advanced_syntax#debug"><code>debug!()</code> macro</a> was introduced to provide the same behavior but in a much briefer form:</p><div class="language-mgs" data-ext="mgs"><pre class="shiki dark-plus" style="background-color:#1E1E1E;" tabindex="0"><code><span class="line"><span style="color:#DCDCAA;">script</span><span style="color:#D4D4D4;"> {</span></span>
<span class="line"><span style="color:#D4D4D4;">  </span><span style="color:#C586C0;">if</span><span style="color:#D4D4D4;"> (</span><span style="color:#9CDCFE;">debug mode</span><span style="color:#D4D4D4;"> is </span><span style="color:#569CD6;">true</span><span style="color:#D4D4D4;">) {</span></span>
<span class="line"><span style="color:#D4D4D4;">    </span><span style="color:#569CD6;">show</span><span style="color:#D4D4D4;"> </span><span style="color:#9CDCFE;">serial dialog</span><span style="color:#D4D4D4;"> { </span><span style="color:#CE9178;">&quot;DEBUG INFO!&quot;</span><span style="color:#D4D4D4;"> }</span></span>
<span class="line"><span style="color:#D4D4D4;">  }</span></span>
<span class="line"><span style="color:#D4D4D4;">  </span><span style="color:#6A9955;">// these are equivalent</span></span>
<span class="line"><span style="color:#D4D4D4;">  </span><span style="color:#DCDCAA;">debug!</span><span style="color:#D4D4D4;">(</span><span style="color:#CE9178;">&quot;DEBUG INFO&quot;</span><span style="color:#D4D4D4;">)</span></span>
<span class="line"><span style="color:#D4D4D4;">}</span></span>
<span class="line"></span></code></pre><div class="highlight-lines"><br><div class="highlight-line"> </div><div class="highlight-line"> </div><div class="highlight-line"> </div><br><div class="highlight-line"> </div><br></div></div><h4 id="debug-commands" tabindex="-1"><a class="header-anchor" href="#debug-commands" aria-hidden="true">#</a> Debug Commands</h4><p>You can register debug <a href="commands">commands</a> in a map&#39;s <a href="script_slots#on-load"><code>on_load</code></a>, and if you put these registrations behind a check for debug mode, they won&#39;t get in the way of a play tester&#39;s experience.</p><p>We&#39;ve found it useful to include debug logging when such a command is registered — both the name and a brief description of what the command does — so you won&#39;t have trouble remember which commands you&#39;ve prepared for a given map.</p><h3 id="debug-techniques" tabindex="-1"><a class="header-anchor" href="#debug-techniques" aria-hidden="true">#</a> Debug Techniques</h3><h4 id="cutscene-skippers" tabindex="-1"><a class="header-anchor" href="#cutscene-skippers" aria-hidden="true">#</a> Cutscene Skippers</h4><p>When debugging later segments of the game, it&#39;s helpful to be able to trigger a script that bypasses otherwise-mandatory <a href="techniques/cutscenes">cutscenes</a>. Such debug scripts should carefully mirror their real counterparts in terms of <a href="variables#save-flags">save flags</a> set and the like, or you might find yourself having to debug the debuggers.</p><h4 id="cutscene-restorers" tabindex="-1"><a class="header-anchor" href="#cutscene-restorers" aria-hidden="true">#</a> Cutscene Restorers</h4><p>Likewise, it sometimes helps to be able to play a cutscene over again. Or, if most or all <a href="techniques/cutscenes">cutscenes</a> have been bypassed, it helps to turn on a specific one separately.</p><h4 id="clean-wipe" tabindex="-1"><a class="header-anchor" href="#clean-wipe" aria-hidden="true">#</a> Clean Wipe</h4><p>When using scripts to emulate a fresh game state, be sure you have a good list of the <a href="variables#save-flags">save flags</a> and <a href="variables#integer-variables">integer variables</a> (etc.) you have been using.</p><h4 id="puzzle-solvers" tabindex="-1"><a class="header-anchor" href="#puzzle-solvers" aria-hidden="true">#</a> Puzzle Solvers</h4><p>While some puzzles can be simplified to accelerate play testing (such as naming the main character &quot;Bub&quot; when they will later need to be named &quot;Bob&quot;), it&#39;s much faster to make scripts to solve puzzles for you.</p><p>By the end of game development of Chapter 1 of the Black Mage Game, there was a &quot;Debug Exa&quot; capable of solving or partially solving the majority of puzzles.</p><h4 id="choreography-controller" tabindex="-1"><a class="header-anchor" href="#choreography-controller" aria-hidden="true">#</a> Choreography Controller</h4><p>Whenever you have a small segment of choreography you want to polish, it helps to split the sequence into a separate script that you can trigger at will.</p><h2 id="vector-view" tabindex="-1"><a class="header-anchor" href="#vector-view" aria-hidden="true">#</a> Vector View</h2><p>One of the in-game debug tools, triggered by pressing <code>XOR</code> and <code>MEM0</code> (the top two buttons on each side of the screen) at the same time. On desktop, press <code>F1</code> and <code>F5</code> instead.</p><p>NOTE: Currently, vector view cannot be toggled when <a href="hex_editor">hex editor</a> <a href="actions/SET_HEX_EDITOR_STATE">control</a> is off.</p><h3 id="visible-vectors" tabindex="-1"><a class="header-anchor" href="#visible-vectors" aria-hidden="true">#</a> Visible Vectors</h3><h4 id="vector-objects" tabindex="-1"><a class="header-anchor" href="#vector-objects" aria-hidden="true">#</a> Vector Objects</h4><p><a href="vector_objects">Vector objects</a> include:</p><ul><li>vector paths</li><li>vector shapes</li><li>vector points (displayed as Xs)</li></ul><p>These lines are green ordinarily, but will turn red if the player entity is &quot;inside&quot; them.</p><h4 id="entity-vectors" tabindex="-1"><a class="header-anchor" href="#entity-vectors" aria-hidden="true">#</a> Entity Vectors</h4><p>Entity hit boxes are half the length and half the height of the <a href="entities">entity</a>&#39;s tile size, and are positioned at the bottom-center of the sprite. This box is green by default, but the entity that was most recently <a href="script_slots#on-interact">interacted</a> with (either with the interact button or hack button) will have a red hitbox instead.</p><p>The entity&#39;s position is considered to be the center of its hitbox as defined above, and is marked by a blue X.</p><h4 id="interaction-hitboxes" tabindex="-1"><a class="header-anchor" href="#interaction-hitboxes" aria-hidden="true">#</a> Interaction Hitboxes</h4><p>When the player presses the hack button or <a href="script_slots#on-interact">interact</a> button, a rectangle is cast in front of the player entity. After a successful interaction, the hitbox will be blue, and will be yellow otherwise.</p><h4 id="collision-vectors" tabindex="-1"><a class="header-anchor" href="#collision-vectors" aria-hidden="true">#</a> Collision Vectors</h4><p>Tiles on the map will have their <a href="tilesets#tile-collisions">vector collision shapes</a> drawn in green, unless the engine has determined they are close enough to the player that collision for that tile will need to be checked, after which they will be drawn in yellow. Red lines indicate a collision was detected on that tile.</p><p>The player&#39;s collision spokes (drawn in purple) are projected in front of the player entity. When they cross a tile&#39;s collision geometry, a collision is detected, and the corresponding knockback vector is drawn as a red line extending in the opposite direction.</p><h4 id="collision-details" tabindex="-1"><a class="header-anchor" href="#collision-details" aria-hidden="true">#</a> Collision Details</h4><p>In the upper-left corner of the screen is a more detailed view of the actual math behind the collision algorithm. Map makers creating original scenarios can safely ignore this part of vector view.</p>`,46),n=[o];function r(h,l){return a(),t("div",null,n)}const c=e(i,[["render",r],["__file","debug_tools.html.vue"]]);export{c as default};

import{_ as e,o as t,c as a,a as i}from"./app-7-tBH-Mq.js";const n={},o=i('<h1 id="hiding-an-entity" tabindex="-1"><a class="header-anchor" href="#hiding-an-entity" aria-hidden="true">#</a> Hiding an Entity</h1><p>There are several ways to hide an <a href="../entities">entity</a>:</p><ol><li>Teleport it off the visible <a href="../maps">map</a>. <ul><li>In Tiled, place a <a href="../maps/vector_objects">vector object</a> off the top-left corner of the map, in the void beyond the map tiles. Teleport entities here to hide them.</li><li>The vector object could be anywhere, really, but due to <a href="../maps/coordinate_overflow">coordinate wrapping</a>, the coordinates beyond the top-left corner are actually very, very large numbers, which might make it easier to keep them hidden. (It will at least make things more interesting for a player who is poking around.)</li></ul></li><li>Teleport them behind something opaque.</li><li>Change them into an <a href="../entities/null_entity">invisible tile</a>.</li></ol><p>After hiding them, you might optionally change their name to something else and change all their <a href="../scripts/script_slots">script slots</a> scripts to <a href="../scripts/null_script">null_script</a>, particularly if there&#39;s a danger for the player to interact with them on accident.</p><p>No matter what, the player will be able to manipulate hidden entities, as they are never properly &quot;unloaded.&quot; This is somewhat authentic in terms of how a real game might handle a similar problem, but if you really, truly want to deny the player access to them, you only have two choices:</p><ol><li>Make a parallel map without them.</li><li><a href="../techniques/actors">Repurpose</a> the entity into another.</li></ol><h2 id="permanently-hiding-entities" tabindex="-1"><a class="header-anchor" href="#permanently-hiding-entities" aria-hidden="true">#</a> Permanently Hiding Entities</h2><p>When a map is loaded, entities will spawn in their default state. This includes their map coordinates. In other words, entities you have previously hidden will not remain hidden after the map is reloaded.</p><p>If you want an entity to be hidden permanently, you must <a href="../techniques/chains_of_small_checks">manage this</a> with the map&#39;s <a href="../scripts/on_load"><code>on_load</code></a> and use a <a href="../scripts/save_flags">save flag</a> to determine whether they are hiding.</p><h2 id="un-hiding-an-entity" tabindex="-1"><a class="header-anchor" href="#un-hiding-an-entity" aria-hidden="true">#</a> Un-Hiding an Entity</h2><p>If an entity only shows up part way through the game, you must include it in the map from the start and un-hide it at the correct time.</p><p>Depending on your needs, you might either:</p><ol><li>Have the entity offscreen (in the void) by default, and have the map&#39;s <a href="../scripts/on_load"><code>on_load</code></a> teleport it to the correct place if <a href="../scripts/save_flags">conditions</a> have been met.</li><li>Have the entity in the correct place by default, but have the map&#39;s <code>on_load</code> teleport it offscreen if <a href="../scripts/save_flags">conditions</a> have <em>not</em> been met.</li></ol>',13),r=[o];function h(l,s){return t(),a("div",null,r)}const c=e(n,[["render",h],["__file","hiding_an_entity.html.vue"]]);export{c as default};
